1. Сгенерировать проект backend на NestJs.
Добавить endpont ping GET
Генерируется uuid, отправляется на фронт и сохраняется в сторадж локальный. И создается контекст (граф). 

Также в business.json лежит список вопросов (упростить для теста, бэкенд должен сохранить в графе - уже есть класс для него в core - всю полученную информацию)

Генерируется сокет соединение с:
message {message: string} => (и ИИ должен текст передавать в ответе - следующий вопрос или чтото такое) 
в system-test пример взавимодействия с агентом. Нужен HR.txt

2. web-ui 
Перенеси сюда свой проект
Заходим -> ping -> пока что лоадер стейт, когда бэк ответ вернул можем уже писать в чат -> /message POST -> и в процессе диалога собираем инфу.
Понять как трекать что диалог с ИИ завершен.